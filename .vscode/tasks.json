{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-lab3",
            "type": "shell",
            "command": "make cleanlab3 && make lab3",
            "args": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "clean",
            "type": "shell",
            "command": "make clean",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}