lab2
初步想法是建立一个S-SDD分析，每个文法符号仅有多个综合属性，属性文法通过属性和对应规则计算获得上下文信息
属性存在节点中，建立一个表，表中存的是每个符号的不同属性内容，而语义错误检查发生在查表中，一旦发现没有对应的表则报错

todo_list:
    1. n重新更改节点
    2. 引入语义规则计算，由于采用S-SDD,所以使用LR分析最方便，只可能在规约是计算语义，因而在规约是加入语义计算接口
    3. 维护一个表用于存储每个符号的变量及其对应规则,表的初步想法是一个hashtable,基本采用指导书中办法
    4. 向add_node中u加入一个analyze分析函数，每次加入时分析
    5. 创建一个hashtable,其中嵌入属性，通过hash function 来映射
    6. 记录标识符的名称、类型、作用域、内存地址等属性。


可能要和上课区别开来，建议建立树的时专注于计算语义，而树建立好之后可以分析，这个时候字符表已经建立了